version: "3.9"

services:
  backend:
    build: ./backend
    container_name: arias_backed
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend/app:/code/app
    ports:
      - "8000:8000"
